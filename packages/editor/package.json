{
  "name": "@tcw/editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@tcw/shared": "*",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
